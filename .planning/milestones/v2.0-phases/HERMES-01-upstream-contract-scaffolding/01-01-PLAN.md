---
phase: 01-upstream-contract-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - plugin.yaml
  - src/chatlytics_hermes/__init__.py
  - src/chatlytics_hermes/adapter.py
  - tests/test_register.py
  - src/chatlytics_adapter/__init__.py        # DELETE
  - src/chatlytics_adapter/actions.py         # DELETE
  - src/chatlytics_adapter/adapter.py         # DELETE
  - tests/test_adapter.py                     # DELETE
  - tests/test_action_parity.py               # DELETE
autonomous: true
requirements:
  - HERMES-01-AC-1
  - HERMES-01-AC-2
  - HERMES-01-AC-3
  - HERMES-01-AC-4
  - HERMES-01-AC-5
must_haves:
  truths:
    - "Importing chatlytics_hermes yields a callable `register` symbol."
    - "`register(MockCtx())` calls `ctx.register_platform(name='chatlytics', ...)` exactly once."
    - "`plugin.yaml` is valid YAML and declares the chatlytics plugin manifest with requires_env BASE_URL + API_KEY."
    - "`pyproject.toml` declares the `hermes_agent.plugins` entry point `chatlytics = chatlytics_hermes:register`."
    - "The v1.x `chatlytics_adapter` package and its tests are entirely gone."
    - "`pip install -e .` (with `hermes-agent>=0.14,<0.15` already installed, or available from GitHub) succeeds."
  artifacts:
    - src/chatlytics_hermes/__init__.py
    - src/chatlytics_hermes/adapter.py
    - plugin.yaml
    - tests/test_register.py
    - pyproject.toml (modified — version=2.0.0, hermes-agent pin, entry point)
  key_links:
    - "pyproject.toml entry point → src/chatlytics_hermes/__init__.py → register → adapter.ChatlyticsAdapter"
    - "plugin.yaml requires_env CHATLYTICS_BASE_URL/CHATLYTICS_API_KEY ↔ adapter `__init__` reads same via PlatformConfig.extra"
---

<objective>
Lay the structural contract that lets `hermes-agent>=0.14,<0.15` discover and load the chatlytics platform plugin. NO outbound, NO inbound, NO media, NO tools — those are HERMES-02/03/04/05. This phase delivers the entry-point wiring + abstract adapter skeleton + plugin.yaml manifest, and irreversibly removes the v1.x standalone-shim tree (`src/chatlytics_adapter/` + its tests).

Purpose: Establish a green plugin baseline against the v0.14.0 plugin contract so subsequent phases can fill in real behavior method-by-method without fighting structural mistakes.

Output: 4 created files, 1 modified file (`pyproject.toml`), 5 deleted paths (v1.x tree). The test `tests/test_register.py::test_register_adds_chatlytics_platform` proves the entry point wires through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/HERMES-01-upstream-contract-scaffolding/01-CONTEXT.md
@pyproject.toml

<canonical_refs>
Authoritative upstream — `/tmp/hermes-ref-v0.14.0/` (fresh shallow clone of `NousResearch/hermes-agent@v2026.5.16`). DO NOT use `D:/docker/waha-oc-plugin/.hermes-ref/` (v0.11.0, pre-plugin-system, WRONG).

Key files the executor must consult while implementing:
- `/tmp/hermes-ref-v0.14.0/plugins/platforms/irc/__init__.py` — minimal `from .adapter import register; __all__ = ["register"]`
- `/tmp/hermes-ref-v0.14.0/plugins/platforms/irc/adapter.py` — full reference. Class `IRCAdapter(BasePlatformAdapter)` at line 96. `register(ctx)` at line 927 — copy the shape, narrow the hook surface for HERMES-01.
- `/tmp/hermes-ref-v0.14.0/plugins/platforms/irc/plugin.yaml` — requires_env / optional_env schema, rich-dict entries with name/description/prompt/password.
- `/tmp/hermes-ref-v0.14.0/gateway/platforms/base.py` — `BasePlatformAdapter` (line 1265). `__init__(self, config: PlatformConfig, platform: Platform)`. Abstract methods: `connect() -> bool` (1539), `disconnect() -> None` (1548), `send(chat_id, content, reply_to=None, metadata=None) -> SendResult` (1553). Non-abstract defaults exist for `send_typing`, `send_image`, `send_voice`, `send_video`, `send_document`, `send_animation`, `send_image_file`, `get_chat_info` — leave those alone; HERMES-02/04 will override.
- `/tmp/hermes-ref-v0.14.0/gateway/config.py` — `Platform` enum (line 100). `_missing_` accepts unknown names dynamically, so `Platform("chatlytics")` works without modifying upstream. `PlatformConfig` at line 281 — `enabled, token, api_key, home_channel, extra` fields.
- `/tmp/hermes-ref-v0.14.0/gateway/platforms/ADDING_A_PLATFORM.md` — Plugin Path section (top of file) is the authoritative third-party guide. Lists all optional `register_platform` hooks; HERMES-01 uses only the minimum subset.
</canonical_refs>

<interfaces>
From `/tmp/hermes-ref-v0.14.0/gateway/platforms/base.py`:
```python
class BasePlatformAdapter(ABC):
    def __init__(self, config: PlatformConfig, platform: Platform): ...
    @abstractmethod
    async def connect(self) -> bool: ...
    @abstractmethod
    async def disconnect(self) -> None: ...
    @abstractmethod
    async def send(
        self,
        chat_id: str,
        content: str,
        reply_to: Optional[str] = None,
        metadata: Optional[Dict[str, Any]] = None,
    ) -> SendResult: ...
```

From `/tmp/hermes-ref-v0.14.0/plugins/platforms/irc/adapter.py` (line 927 — copy the SHAPE, NOT the IRC-specific hooks):
```python
def register(ctx):
    ctx.register_platform(
        name="irc",
        label="IRC",
        adapter_factory=lambda cfg: IRCAdapter(cfg),
        # ... many optional hooks: check_fn, validate_config, is_connected,
        # required_env, install_hint, setup_fn, env_enablement_fn,
        # cron_deliver_env_var, standalone_sender_fn, allowed_users_env,
        # allow_all_env, max_message_length, emoji, platform_hint, ...
    )
```

For HERMES-01, the `register` call uses ONLY:
`name`, `label`, `adapter_factory`, `required_env`, `install_hint`, `emoji`, `platform_hint`. All other hooks (check_fn, validate_config, env_enablement_fn, standalone_sender_fn, cron_deliver_env_var, etc.) are explicitly deferred to HERMES-02/03/04/05 and MUST NOT appear in this phase's `register()`.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove v1.x tree + scaffold new package skeleton + plugin.yaml</name>
  <files>
    src/chatlytics_adapter/__init__.py (DELETE),
    src/chatlytics_adapter/actions.py (DELETE),
    src/chatlytics_adapter/adapter.py (DELETE),
    tests/test_adapter.py (DELETE),
    tests/test_action_parity.py (DELETE),
    src/chatlytics_hermes/__init__.py (CREATE),
    src/chatlytics_hermes/adapter.py (CREATE),
    plugin.yaml (CREATE)
  </files>
  <action>
    Git-remove the entire `src/chatlytics_adapter/` directory (operator decision: NO compat shim, never published). Also git-remove `tests/test_adapter.py` and `tests/test_action_parity.py` — they target deleted classes and will be replaced phase-by-phase. Use `git rm -r src/chatlytics_adapter tests/test_adapter.py tests/test_action_parity.py` so removals are staged.

    Create `src/chatlytics_hermes/__init__.py` matching the IRC plugin pattern at `/tmp/hermes-ref-v0.14.0/plugins/platforms/irc/__init__.py`: a two-line module that does `from .adapter import register` and sets `__all__ = ["register"]`. No additional logic.

    Create `src/chatlytics_hermes/adapter.py` with:
    - Module docstring referencing the Chatlytics WhatsApp gateway and noting that HERMES-01 scaffolds the contract; HERMES-02/03/04 fill in behavior.
    - Imports: `from typing import Any, Dict, Optional`, `from gateway.platforms.base import BasePlatformAdapter, SendResult`, `from gateway.config import Platform, PlatformConfig`. Keep imports at module level (matches IRC plugin). NO functional logic at import time.
    - `class ChatlyticsAdapter(BasePlatformAdapter):` with `__init__(self, config: PlatformConfig, **kwargs)`. Inside `__init__` call `super().__init__(config=config, platform=Platform("chatlytics"))` — uses the `_missing_` dynamic enum extension per `/tmp/hermes-ref-v0.14.0/gateway/config.py:130`, same trick IRC uses.
    - In `__init__`, read `extra = getattr(config, "extra", {}) or {}` then capture `self.base_url`, `self.api_key`, `self.account_id`, `self.webhook_port`, `self.webhook_secret`, `self.home_channel` from env-first/config-fallback (mirror IRC `__init__` lines 105-119). Env var names: `CHATLYTICS_BASE_URL`, `CHATLYTICS_API_KEY`, `CHATLYTICS_ACCOUNT_ID`, `CHATLYTICS_WEBHOOK_PORT` (int, default 8765), `CHATLYTICS_WEBHOOK_SECRET`, `CHATLYTICS_HOME_CHANNEL`. Store but do NOT validate — HERMES-02 owns validation when `connect()` is implemented.
    - `@property name(self) -> str: return "Chatlytics"`.
    - Three abstract methods, each a one-line body raising `NotImplementedError("filled in HERMES-02")` for `connect` and `disconnect`, `NotImplementedError("filled in HERMES-02")` for `send`. Exact signatures match the abstract base (see `<interfaces>` above). No partial implementations.
    - At module bottom, define `def register(ctx) -> None:` that calls `ctx.register_platform(...)` with EXACTLY these keyword arguments and no others:
      - `name="chatlytics"`
      - `label="Chatlytics WhatsApp"`
      - `adapter_factory=lambda cfg: ChatlyticsAdapter(cfg)`
      - `required_env=["CHATLYTICS_BASE_URL", "CHATLYTICS_API_KEY"]`
      - `install_hint="pip install -e git+https://github.com/omernesh/chatlytics-hermes.git"`
      - `emoji="\U0001F4AC"`  (avoid raw emoji in source — use escape; the file may be read on Windows)
      - `platform_hint=("You are chatting via Chatlytics, a WhatsApp gateway. Messages support WhatsApp-flavored "
                       "markdown (bold *text*, italic _text_, strikethrough ~text~, monospace ```text```). "
                       "Keep responses conversational and concise; long messages are split by the gateway.")`
    Do not add other hooks (env_enablement_fn, cron_deliver_env_var, standalone_sender_fn, check_fn, validate_config, max_message_length, etc.) — those land in HERMES-02/03/04 per scope. Explicit minimalism here lets the plan-checker confirm scope discipline.

    Create `plugin.yaml` (project root, NOT inside src/) with the schema documented at `/tmp/hermes-ref-v0.14.0/plugins/platforms/irc/plugin.yaml`. Required keys:
    - `name: chatlytics` (string, lowercase, matches `register_platform(name=...)`)
    - `label: Chatlytics WhatsApp`
    - `kind: platform`
    - `version: 2.0.0`
    - `description:` (block scalar — one sentence, "Chatlytics WhatsApp gateway adapter for Hermes Agent. Connects Hermes to Chatlytics's REST API for inbound webhooks and outbound messages/media.")
    - `author: Chatlytics`
    - `requires_env:` list with two rich-dict entries:
      - `{ name: CHATLYTICS_BASE_URL, description: "Chatlytics gateway base URL (e.g. https://gateway.chatlytics.ai)", prompt: "Chatlytics base URL", password: false }`
      - `{ name: CHATLYTICS_API_KEY, description: "API key for Chatlytics REST authentication (Bearer token)", prompt: "Chatlytics API key", password: true }`
    - `optional_env:` list with rich-dict entries for: `CHATLYTICS_ACCOUNT_ID` (description: "Default session/account ID for outbound sends", password: false), `CHATLYTICS_WEBHOOK_PORT` (description: "Local port for the aiohttp webhook listener (default: 8765, filled in HERMES-03)", password: false), `CHATLYTICS_WEBHOOK_SECRET` (description: "HMAC-SHA256 shared secret for X-Chatlytics-Signature verification (HERMES-03)", password: true), `CHATLYTICS_HOME_CHANNEL` (description: "Default chat_id for cron/notification delivery (HERMES-04)", password: false).

    Verify the manifest YAML loads via `python -c "import yaml; yaml.safe_load(open('plugin.yaml'))"` before moving on.
  </action>
  <verify>
    <automated>
      cd "D:/docker/chatlytics-hermes-split" && \
      test ! -e src/chatlytics_adapter && \
      test ! -e tests/test_adapter.py && \
      test ! -e tests/test_action_parity.py && \
      test -f src/chatlytics_hermes/__init__.py && \
      test -f src/chatlytics_hermes/adapter.py && \
      test -f plugin.yaml && \
      python -c "import yaml; m = yaml.safe_load(open('plugin.yaml')); assert m['name'] == 'chatlytics' and m['kind'] == 'platform' and m['version'] == '2.0.0'; assert {e['name'] for e in m['requires_env']} == {'CHATLYTICS_BASE_URL', 'CHATLYTICS_API_KEY'}"
    </automated>
  </verify>
  <done>
    v1.x `chatlytics_adapter` package + its two tests are git-removed. New package skeleton lives at `src/chatlytics_hermes/{__init__.py,adapter.py}`. `plugin.yaml` is valid YAML with `name=chatlytics`, `kind=platform`, `version=2.0.0`, and the two required env entries. Module can be lint-loaded (`python -c "import ast; ast.parse(open('src/chatlytics_hermes/adapter.py').read())"`) — full runtime import is exercised in Task 3 after pyproject is updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pyproject.toml — v2.0.0 deps + entry point</name>
  <files>pyproject.toml</files>
  <action>
    Rewrite `pyproject.toml` in place. Preserve the existing `[build-system]` block (`requires = ["setuptools>=68"]`, `build-backend = "setuptools.build_meta"`).

    In the `[project]` table:
    - `version = "2.0.0"` (was 1.1.0).
    - `description = "Chatlytics WhatsApp platform plugin for Hermes Agent (v0.14+)"` (replace the v1.x adapter wording).
    - Keep `name = "chatlytics-hermes"`, `license`, `readme`, `requires-python = ">=3.10"`, `authors`, `keywords`, `classifiers` intact.
    - Replace `dependencies = ["httpx>=0.24", "flask>=3.0"]` with:
      ```
      dependencies = [
        "hermes-agent>=0.14,<0.15",
        "httpx>=0.27,<1",
        "aiohttp>=3.9,<4",
        "PyYAML>=6.0",
      ]
      ```
      Reasoning: drop flask entirely (HERMES-03 uses aiohttp), pin hermes-agent per ROADMAP D-04, pin httpx per existing convention, add aiohttp now so HERMES-03 has the dep available (cheaper to install once), add PyYAML because Task 1 verification + `tests/test_register.py` use `yaml.safe_load(open('plugin.yaml'))`.

    In `[project.optional-dependencies]`:
    - Rename `test` → `dev` and include: `pytest>=8.0`, `pytest-asyncio>=0.23`, `respx>=0.21`. (`respx` carried forward — HERMES-02 onward exercises httpx mocking. No change to test surface in this phase, but keeping it avoids a follow-up edit.)

    Add a new top-level table:
    ```
    [project.entry-points."hermes_agent.plugins"]
    chatlytics = "chatlytics_hermes:register"
    ```
    This is the canonical discovery mechanism per ADDING_A_PLATFORM.md Plugin Path and is the LITERAL string acceptance criterion 5 greps for.

    Add a `[tool.setuptools.packages.find]` block scoped to `where = ["src"]` so the rename from `chatlytics_adapter` to `chatlytics_hermes` is picked up by `pip install -e .`. Without this, setuptools auto-discovery against `src/` may pick up stale paths during the transition.

    Add a `[tool.pytest.ini_options]` block: `asyncio_mode = "auto"`, `pythonpath = ["src"]`. The pythonpath entry lets `pytest tests/` resolve `chatlytics_hermes` even before `pip install -e .` completes; the asyncio_mode default unblocks HERMES-02/03 tests later.

    Keep `[project.urls]` Homepage + Repository.
  </action>
  <verify>
    <automated>
      cd "D:/docker/chatlytics-hermes-split" && \
      python -c "import tomllib; d = tomllib.load(open('pyproject.toml','rb')); p = d['project']; assert p['version']=='2.0.0'; assert any(dep.startswith('hermes-agent>=0.14') for dep in p['dependencies']); assert all(not dep.startswith('flask') for dep in p['dependencies']); ep = d['project']['entry-points']['hermes_agent.plugins']; assert ep['chatlytics']=='chatlytics_hermes:register'; print('OK')" && \
      grep -F '[project.entry-points."hermes_agent.plugins"]' pyproject.toml && \
      grep -F 'chatlytics = "chatlytics_hermes:register"' pyproject.toml
    </automated>
  </verify>
  <done>
    `pyproject.toml` declares version=2.0.0, drops flask, pins hermes-agent>=0.14,<0.15 + httpx + aiohttp + PyYAML, exposes the canonical `[project.entry-points."hermes_agent.plugins"]` block, and includes setuptools/pytest config so the new package layout is discoverable. The literal string `chatlytics = "chatlytics_hermes:register"` is present and greppable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author test_register.py + run acceptance criteria 1-5</name>
  <files>tests/test_register.py</files>
  <action>
    Create `tests/test_register.py` with the following test surface:

    1. `class MockCtx:` — minimal stand-in for the Hermes plugin context. Holds `self.platforms: list[dict] = []`. Implements `def register_platform(self, **kwargs) -> None: self.platforms.append(kwargs)`. NO other methods (the register() in HERMES-01 must not call anything else; later phases will extend MockCtx for tools, env-enablement, etc.).

    2. `def test_register_is_callable():` — `from chatlytics_hermes import register; assert callable(register); assert register.__name__ == "register"`. This is acceptance criterion 1 in test form.

    3. `def test_register_adds_chatlytics_platform():` — the ROADMAP-named test (criterion 2). Construct `ctx = MockCtx()`, call `register(ctx)`. Assert `len(ctx.platforms) == 1`. Pull `entry = ctx.platforms[0]` and assert:
       - `entry["name"] == "chatlytics"`
       - `entry["label"] == "Chatlytics WhatsApp"`
       - `callable(entry["adapter_factory"])`
       - `entry["required_env"] == ["CHATLYTICS_BASE_URL", "CHATLYTICS_API_KEY"]`
       - `entry["install_hint"].startswith("pip install")`
       - `"platform_hint" in entry` and the hint mentions "Chatlytics" or "WhatsApp"
       Do NOT assert on hooks not yet declared (env_enablement_fn, standalone_sender_fn, etc.); those are HERMES-02/04 concerns.

    4. `def test_plugin_yaml_is_valid():` — covers acceptance criterion 3. `import yaml`, `from pathlib import Path`; `manifest = yaml.safe_load((Path(__file__).resolve().parents[1] / "plugin.yaml").read_text())`. Assert `manifest["name"] == "chatlytics"`, `manifest["kind"] == "platform"`, `manifest["version"] == "2.0.0"`, and `{e["name"] for e in manifest["requires_env"]} == {"CHATLYTICS_BASE_URL", "CHATLYTICS_API_KEY"}`.

    5. `def test_pyproject_declares_hermes_entry_point():` — covers criterion 5 in test form (the grep in Task 2 covers the literal-string assertion; this test ensures the dict structure is also right). `import tomllib`, `from pathlib import Path`; `data = tomllib.loads((Path(__file__).resolve().parents[1] / "pyproject.toml").read_text())`. Assert `data["project"]["entry-points"]["hermes_agent.plugins"]["chatlytics"] == "chatlytics_hermes:register"`. Also assert `"hermes-agent>=0.14" in dep` for some dep in `data["project"]["dependencies"]`.

    NOTE: Do NOT exercise `adapter_factory(cfg)` in this phase — instantiating `ChatlyticsAdapter` requires `gateway.platforms.base` from `hermes-agent`, and acceptance criterion 4 (clean-venv install with hermes-agent==0.14.0) is the integration gate for that. Inside this test file, the `register` function is called with MockCtx, which only inspects the kwargs — no hermes-agent import is triggered yet because `adapter_factory=lambda cfg: ChatlyticsAdapter(cfg)` is a lazy lambda and is never invoked. This keeps the unit-test suite hermes-free for fast iteration. HERMES-02 will add a hermes-required integration test once `connect()` is implemented.

    However, Task 1's `src/chatlytics_hermes/adapter.py` DOES have `from gateway.platforms.base import ...` at module level — which means importing `chatlytics_hermes` requires hermes-agent to be installed for `test_register_is_callable` to pass. Handle this with a conditional import in `adapter.py`: keep `BasePlatformAdapter` / `SendResult` / `Platform` / `PlatformConfig` imports at module level UNLESS that breaks acceptance criterion 1 in an env without hermes-agent. To allow `from chatlytics_hermes import register` to work without hermes-agent installed (criterion 1 explicitly runs `python -c "from chatlytics_hermes import register; print(register.__name__)"` and does not guarantee a hermes env), wrap the upstream imports in a `try/except ImportError` block at module top that sets the symbols to `None` and lets `ChatlyticsAdapter` raise on instantiation. The `register()` function does NOT instantiate the adapter (it just stores the lambda), so it survives the missing-dep case. This is the same lazy-import pattern called out in the IRC adapter's docstring (lines 38-42) — adapt that pattern to `try/except` so static analysis still works.

    Updated `adapter.py` import block:
    ```python
    try:
        from gateway.platforms.base import BasePlatformAdapter, SendResult
        from gateway.config import Platform, PlatformConfig
        _HERMES_AVAILABLE = True
    except ImportError:  # hermes-agent not yet installed (acceptance criterion 1 path)
        BasePlatformAdapter = object  # type: ignore[assignment, misc]
        SendResult = None  # type: ignore[assignment]
        Platform = None  # type: ignore[assignment]
        PlatformConfig = None  # type: ignore[assignment]
        _HERMES_AVAILABLE = False
    ```
    And in `ChatlyticsAdapter.__init__`, guard: `if not _HERMES_AVAILABLE: raise RuntimeError("hermes-agent>=0.14,<0.15 must be installed to instantiate ChatlyticsAdapter")`. This preserves Task 1's intent (the class exists and is the right shape) while letting the bare-import smoke test in criterion 1 pass cleanly. Update the Task 1 file with this import shim now if it wasn't already written that way.

    After writing the test file, run the acceptance criteria sequentially in the project root (Bash tool):
    - AC-1: `python -c "from chatlytics_hermes import register; print(register.__name__)"` must print `register`.
    - AC-2: `pytest tests/test_register.py::test_register_adds_chatlytics_platform -q` must pass.
    - AC-3: `python -c "import yaml; yaml.safe_load(open('plugin.yaml'))"` must succeed.
    - AC-5: `grep -F 'chatlytics = "chatlytics_hermes:register"' pyproject.toml` must match.
    - AC-4 (clean-venv install): run the dockerized smoke per ROADMAP context:
      `docker run --rm -v "/d/docker/chatlytics-hermes-split:/work" -w /work python:3.13-slim sh -c "pip install --quiet hermes-agent>=0.14,<0.15 || pip install --quiet 'hermes-agent @ git+https://github.com/NousResearch/hermes-agent.git@v2026.5.16' ; pip install --quiet -e .[dev] && python -c 'from chatlytics_hermes import register; print(register.__name__)'"`
      If the PyPI install fails because hermes-agent 0.14.x is not yet published (per ROADMAP context note: v0.13.0 is the latest published as of 2026-05-17), the `||` fallback installs from the GitHub tag. EITHER path must finish with `register` printed. Record actual install path in the commit message so HERMES-06 README knows which install vector to document.
    Also run the full `pytest tests/test_register.py -q` to confirm tests 2, 4, 5 all pass.
  </action>
  <verify>
    <automated>
      cd "D:/docker/chatlytics-hermes-split" && \
      python -c "from chatlytics_hermes import register; print(register.__name__)" | grep -q '^register$' && \
      pytest tests/test_register.py -q && \
      python -c "import yaml; yaml.safe_load(open('plugin.yaml'))" && \
      grep -F 'chatlytics = "chatlytics_hermes:register"' pyproject.toml
    </automated>
  </verify>
  <done>
    All 5 acceptance criteria from ROADMAP Phase 1 pass non-interactively. `tests/test_register.py` contains 4 named tests (callable, register-adds-platform, plugin-yaml-valid, pyproject-entry-point) all green. Clean-venv docker smoke (criterion 4) exits 0 via either PyPI or GitHub install path; commit message records which vector worked. No test depends on hermes-agent being installed in the host venv — the bare-import smoke uses a try/except shim in `adapter.py`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin discovery → hermes runtime | hermes-agent imports `chatlytics_hermes:register` via entry point; the `register` callable runs in Hermes's trust domain. |
| pyproject deps → pip resolver | new transitive deps (aiohttp, PyYAML) and the version-pinned hermes-agent become part of the install graph. |
| plugin.yaml → hermes config UI | `requires_env` entries are surfaced to the operator via `hermes config` wizard (per ADDING_A_PLATFORM.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HERMES-01-01 | Tampering | `register()` kwargs (e.g. forged `adapter_factory` returning a hostile object) | accept | Plugin executes in same trust domain as hermes-agent; operator owns the install graph. No untrusted source. |
| T-HERMES-01-02 | Information Disclosure | `CHATLYTICS_API_KEY` env var leak | mitigate | `plugin.yaml` marks `password: true` for API_KEY and WEBHOOK_SECRET so the config wizard masks input. Adapter does not log the key (HERMES-02 enforces redaction). |
| T-HERMES-01-SC | Tampering | pip installs (hermes-agent from GitHub tag) | accept | Operator decision: install vector is pinned to the v2026.5.16 git tag (immutable). HERMES-06 smoke runs the same install in CI. |
| T-HERMES-01-03 | Denial of Service | `ChatlyticsAdapter` instantiated without hermes-agent installed | mitigate | `_HERMES_AVAILABLE` guard raises a clear RuntimeError instead of an opaque ImportError at attribute access time. |
</threat_model>

<verification>
End-of-phase checks (each command must exit 0):

1. `python -c "from chatlytics_hermes import register; print(register.__name__)"` → prints `register`
2. `pytest tests/test_register.py -q` → all 4 tests pass
3. `python -c "import yaml; yaml.safe_load(open('plugin.yaml'))"` → exits 0
4. `docker run --rm -v "/d/docker/chatlytics-hermes-split:/work" -w /work python:3.13-slim sh -c "pip install --quiet 'hermes-agent>=0.14,<0.15' || pip install --quiet 'hermes-agent @ git+https://github.com/NousResearch/hermes-agent.git@v2026.5.16'; pip install --quiet -e .[dev] && python -c 'from chatlytics_hermes import register; print(register.__name__)'"` → exits 0, prints `register`
5. `grep -F 'chatlytics = "chatlytics_hermes:register"' pyproject.toml` → matches
6. `test ! -e src/chatlytics_adapter && test ! -e tests/test_adapter.py && test ! -e tests/test_action_parity.py` → all true (v1.x tree fully removed)
</verification>

<success_criteria>
- All 5 ROADMAP Phase 1 acceptance criteria pass autonomously.
- Atomic commits: Task 1 (remove v1.x + scaffold new package + plugin.yaml), Task 2 (pyproject.toml), Task 3 (test_register.py + acceptance run). Each commit message has the required `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer.
- Zero changes to `src/chatlytics_hermes/adapter.py` beyond the skeleton + `register()` (no outbound logic, no inbound logic, no media handlers, no tool registrations — those land in HERMES-02/03/04/05).
- `git status` clean at end of phase (all changes staged + committed).
</success_criteria>

<output>
Create `.planning/phases/HERMES-01-upstream-contract-scaffolding/01-01-SUMMARY.md` when done with:
- Per-task outcome (files touched, commit SHA)
- Acceptance criteria results (PASS/FAIL with command output snippets)
- The install vector used in the dockerized smoke (PyPI vs git tag)
- Any deviations from this plan and rationale
</output>
