---
phase: 06-release-smoke-test
plan: 01
type: execute
wave: 1
depends_on:
  - HERMES-05
files_modified:
  - README.md                         # REWRITE -- v2.0 plugin-perspective rewrite
  - CHANGELOG.md                      # PREPEND -- 2.0.0 (2026-05-17) -- BREAKING
  - scripts/smoke.sh                  # CREATE -- dockerized smoke test
  - src/chatlytics_hermes/adapter.py  # MODIFIED -- wrap blocking open()/read() in asyncio.to_thread (04-MED-02 fix)
  - tests/test_media.py               # MODIFIED -- regression test for asyncio.to_thread wrap
  - pyproject.toml                    # VERIFY only -- no changes expected
autonomous: true
requirements:
  - HERMES-06-AC-1   # bash scripts/smoke.sh exits 0
  - HERMES-06-AC-2   # smoke output contains "chatlytics" in entry-point enumeration
  - HERMES-06-AC-3   # pytest tests/ -> 44/44 PASS
  - HERMES-06-AC-4   # README contains 0 occurrences of ChatlyticsAdapter(
  - HERMES-06-AC-5   # CHANGELOG has ## 2.0.0 at top with BREAKING marker
  - HERMES-06-AC-6   # pyproject.toml has version 2.0.0 + entry-point chatlytics
  - HERMES-06-AC-7   # git tag --list v2.0.0 returns the tag (local only)
  - HERMES-06-AC-8   # NO python -m build / twine upload anywhere in phase artifacts
must_haves:
  truths:
    - "NO PyPI publish. NO python -m build. NO twine upload. Operator-locked."
    - "Pushing v2.0.0 tag is OUT OF SCOPE -- create locally only; operator pushes manually."
    - "README rewrite drops ALL v1.x ChatlyticsAdapter(...) constructor snippets."
    - "Smoke uses docker python:3.13-slim + pkg_resources.iter_entry_points('hermes_agent.plugins')."
    - "04-MED-02 fix wraps the blocking open()/read() in _resolve_media_url with asyncio.to_thread."
    - "04-MED-01 (_keep_typing async-cm shape) DOCUMENTED in README Architecture notes, no code change."
    - "Hermes v0.14 CLI provides 'hermes plugins list/ls' but it only lists git-installed plugins, NOT pip entry-point plugins. Smoke MUST use pkg_resources discovery (verified via /tmp/hermes-ref-v0.14.0/hermes_cli/plugins.py:170 ENTRY_POINTS_GROUP = 'hermes_agent.plugins')."
  artifacts:
    - "README.md (REWRITTEN)"
    - "CHANGELOG.md (PREPENDED with 2.0.0 entry)"
    - "scripts/smoke.sh (CREATED, +x)"
    - "src/chatlytics_hermes/adapter.py (asyncio.to_thread wrap in _resolve_media_url)"
    - "tests/test_media.py (concurrency regression test added)"
    - "Git tag v2.0.0 (LOCAL ONLY)"
    - "06-01-SUMMARY.md, 06-VERIFICATION.md, 06-REVIEW.md"
  key_links:
    - "scripts/smoke.sh -- single bash script, exits 0 on full success"
    - "README.md -- 9 sections; no ChatlyticsAdapter( token; v2.0 plugin pattern only"
    - "CHANGELOG.md -- ## 2.0.0 (2026-05-17) -- BREAKING entry prepended above 1.x history"
---

<objective>
Ship v2.0.0 of chatlytics-hermes as a first-class Hermes plugin. Rewrite README, prepend CHANGELOG, create dockerized smoke test, address two carry-forward review action items, tag locally. NO PyPI PUBLISH (operator-locked).
</objective>

<context>
@.planning/phases/HERMES-06-release-smoke-test/06-CONTEXT.md
@README.md
@CHANGELOG.md
@pyproject.toml
@plugin.yaml
@src/chatlytics_hermes/adapter.py
@src/chatlytics_hermes/tools.py
@.planning/phases/HERMES-04-media-ux-polish-cron/04-REVIEW.md
@.planning/phases/HERMES-05-full-chatlytics-tool-surface/05-REVIEW.md

Canonical refs:
- Upstream Hermes v0.14: `/tmp/hermes-ref-v0.14.0/` (tag v2026.5.16)
- Entry-point group: `hermes_agent.plugins` (verified at hermes_cli/plugins.py:170)
- Hermes CLI plugins.py list command exists but only enumerates git-installed plugins -- pip entry-point plugins are NOT shown by `hermes plugins list`. Smoke MUST use `pkg_resources.iter_entry_points(...)`.
</context>

<scope>
## In scope
- README.md FULL REWRITE (drop v1.x, document v2.0 plugin pattern)
- CHANGELOG.md PREPEND ## 2.0.0 (2026-05-17) -- BREAKING entry
- scripts/smoke.sh CREATE (dockerized; entry-point + import + pytest)
- src/chatlytics_hermes/adapter.py: wrap open()/read() with asyncio.to_thread (04-MED-02)
- tests/test_media.py: concurrency regression test (filepath path no longer blocks loop)
- Git tag v2.0.0 LOCAL ONLY (do not push)
- 06-01-SUMMARY.md, 06-VERIFICATION.md, 06-REVIEW.md

## Out of scope
- PyPI publish (LOCKED)
- python -m build / twine upload (LOCKED)
- Pushing v2.0.0 to origin (operator manual action)
- Marketplace listing
- Live Chatlytics integration test
- New tools / adapter behavior changes beyond the 04-MED-02 wrap
- pyproject.toml changes (already correct per HERMES-01..05)
</scope>

<implementation>

## Order of commits (atomic)

### Commit 1 -- `fix(hermes-06): wrap blocking open()/read() in asyncio.to_thread (04-MED-02)`
Files: `src/chatlytics_hermes/adapter.py`, `tests/test_media.py`

Change `_resolve_media_url` so the local-file branch reads via `asyncio.to_thread`:
```python
else:
    path = str(resource)
    def _read_file() -> tuple[bytes, str]:
        with open(path, "rb") as fh:
            return fh.read(), os.path.basename(path) or "upload.bin"
    content, basename = await asyncio.to_thread(_read_file)
    name = upload_filename or basename
    ctype = content_type or _guess_content_type(name)
    upload_response = await self._client.upload_file(
        filename=name, content=content, content_type=ctype
    )
```

Add a regression test that proves the loop is NOT blocked during file reads -- spawn 5 concurrent send_image calls with filepath inputs and assert that an `asyncio.sleep(0)` heartbeat fires interleaved with reads (precise assertion: a sentinel coroutine running in parallel completes within a tight bound that would not be met if the reads were synchronous on a large enough file). Simpler test: monkeypatch `builtins.open` to assert it was called via a worker thread (check `threading.current_thread() is not threading.main_thread()`).

### Commit 2 -- `docs(hermes-06): README v2.0 rewrite`
Files: `README.md`

Full rewrite per Phase 6 spec, 9 sections. Verify `grep -c "ChatlyticsAdapter(" README.md` returns 0.

### Commit 3 -- `docs(hermes-06): CHANGELOG 2.0.0 (BREAKING) entry`
Files: `CHANGELOG.md`

Prepend the `## 2.0.0 (2026-05-17) -- BREAKING` entry per Phase 6 spec.

### Commit 4 -- `chore(hermes-06): scripts/smoke.sh dockerized release verification`
Files: `scripts/smoke.sh` (CREATE, chmod +x)

Single bash script. Runs in docker python:3.13-slim. Installs hermes-agent from Git tag v2026.5.16. Installs the package editable. Asserts:
1. `from chatlytics_hermes import register` -- import works
2. `pkg_resources.iter_entry_points("hermes_agent.plugins")` contains `chatlytics`
3. `pytest tests/ -q` -- 0 failures

Returns 0 on full success; non-zero otherwise.

### Commit 5 -- `chore(hermes-06): tag v2.0.0 (local only)`
No file change in this commit; tag command runs separately AFTER all preceding commits land:
```bash
git tag -a v2.0.0 -m "v2.0.0 -- full Hermes plugin rebuild against hermes-agent v0.14"
```
DO NOT push.

### Commit 6 -- `docs(hermes-06): SUMMARY + VERIFICATION + REVIEW`
Files:
- `.planning/phases/HERMES-06-release-smoke-test/06-01-SUMMARY.md`
- `.planning/phases/HERMES-06-release-smoke-test/06-VERIFICATION.md`
- `.planning/phases/HERMES-06-release-smoke-test/06-REVIEW.md`

</implementation>

<verification>

## Acceptance criteria mapping
| AC | Asserts |
|---|---|
| 1 | `bash scripts/smoke.sh` exits 0 |
| 2 | Smoke output contains `chatlytics` in entry-point enumeration |
| 3 | pytest tests/ -- 0 failures (44 tests + 1 new concurrency test = 45) |
| 4 | `grep -c "ChatlyticsAdapter(" README.md` returns 0 |
| 5 | CHANGELOG.md line 1-2 contains `## 2.0.0` and `BREAKING` |
| 6 | pyproject.toml version=2.0.0 and `chatlytics = "chatlytics_hermes:register"` present |
| 7 | `git tag --list v2.0.0` returns `v2.0.0` |
| 8 | `git log --all -p \| grep -E "(twine upload\|python -m build)"` returns nothing (verified via grep over phase commits + scripts/) |

## Smoke invocation
```bash
bash scripts/smoke.sh
```
Expected: PASS -- entry-point OK, 45/45 tests, exit 0.

## Regression guard
- All 44 prior tests still pass (HERMES-01..05).
- `_resolve_media_url` bytes path unchanged.
- `_resolve_media_url` http(s) URL path unchanged.
- Only the local-file branch is wrapped.
</verification>

<followups>
- Push v2.0.0 tag: `git push origin v2.0.0` (OPERATOR ACTION)
- Future milestone: PyPI publish (1-command `python -m build && twine upload dist/*` -- OPERATOR DECIDES WHEN)
- 04-MED-01 (`_keep_typing` async-cm shape): DOCUMENTED in README "Architecture notes"; no upstream PR planned
- 05-LOW-02 (looksLikeJid regex at schema validation): deferred to v2.1+ if false-positive volume warrants
- 02-LOW-02 (send_typing log flood under concurrency): no tools call _keep_typing; revisit if tool latency profile shifts
</followups>
