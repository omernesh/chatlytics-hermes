---
milestone: v2.1
milestone_name: Critical safety fixes + tech debt resolution + live-loader integration
audited: 2026-05-17
audited_by: gsd-audit-milestone (claude-opus-4-7-1m)
status: passed
scores:
  phases_passed: 6/6
  tests_passing: 88/88
  v2_0_findings_closed: see closures table
  cross_phase_integration: verified (live-loader test passes against in-memory PluginContext)
  invariants_preserved: 8/8
gaps: {}
tech_debt:
  - phase: HERMES-09
    items:
      - "LOW-01 deferred (cosmetic log-level nits — non-blocking)"
      - "INFO-02..04 deferred (style nits)"
  - phase: HERMES-10
    items:
      - "LOW-02 deferred (cosmetic)"
      - "INFO-01..03 deferred (style nits)"
      - "Sentinel `_error` key on get_chat_info — would be breaking, deferred to v2.2+"
      - "Strict JID regex enforcement — would break phone numbers/display names, deferred to v2.2+"
      - "Collapsing send_image / send_image_file into one method — breaking, deferred to v2.2+"
  - phase: HERMES-11
    items:
      - "PR-MED-03 partial: --retries 3 in; long-term wheel caching deferred (not actionable in v2.1)"
  - phase: cross-cutting
    items:
      - "v2.0.0 local tag still points at known-broken artifact (BL-01 pre-fix). Operator may delete after v2.1.0 push."
deferred_to_v2_2: true
deferred_items_safe_to_defer: true
---

# v2.1 Milestone Audit

**Verdict: PASSED.** All 6 phases (HERMES-07 through HERMES-12) verified independently and pass their acceptance criteria. 88/88 tests green in env-clean run. The BLOCKER + 2 HIGHs from `.planning/v2.0-MILESTONE-CODE-REVIEW.md` are fixed and locked under regression tests. v2.1.0 tag created locally (operator push pending — operator lock preserved). Tech debt is bounded, classified, and explicitly deferred to v2.2+; nothing in the deferred set is a blocker for shipping v2.1.0.

## 1. Milestone scope

Per `.planning/ROADMAP.md` v2.1 section — six sequential phases:

| Phase | Theme | Status |
|-------|-------|--------|
| HERMES-07 | Live-loader integration smoke (surfaces BL-01) | passed |
| HERMES-08 | Critical safety fixes — BL-01 + HI-01 + HI-03 + async lifecycle | passed |
| HERMES-09 | Observability + log hygiene | passed |
| HERMES-10 | Input validation + UX alignment | passed |
| HERMES-11 | Test infra cleanup | passed |
| HERMES-12 | Release v2.1.0 (LOCAL tag only) | passed |

All six phase directories under `.planning/phases/HERMES-{07..12}-*/` contain CONTEXT, PLAN, REVIEW (Phase 10 additionally has REVIEW-FIX), and VERIFICATION files. No phase is unverified.

## 2. v2.0 findings closure traceability

This milestone was driven by findings carried forward from two v2.0 reviews:

- `.planning/v2.0-MILESTONE-CODE-REVIEW.md` — 1 BLOCKER, 3 HIGH, 5 MED, 7 LOW, 3 INFO
- `.planning/v2.0-MILESTONE-PR-REVIEW.md` — 0 BLOCKER, 0 HIGH, 4 MED, 7 LOW, 5 INFO

### Critical fixes (BLOCKER + HIGH)

| Finding | Severity | Phase | Status | Evidence |
|---------|----------|-------|--------|----------|
| BL-01 — `_keep_typing` crashes inbound base path | BLOCKER | HERMES-07 (reproduce) + HERMES-08 (fix) | CLOSED | `test_live_loader.py::test_base_process_message_invokes_keep_typing` xfail→PASS; `test_keep_typing_is_a_coroutine` xfail→PASS; `test_bl01_keep_typing_accepts_metadata_kwarg` xfail→PASS |
| HI-01 — `filePath` arbitrary file read | HIGH | HERMES-07 (reproduce) + HERMES-08 (fix) | CLOSED | `test_hi01_send_file_rejects_path_outside_allowed_roots` xfail→PASS; `CHATLYTICS_UPLOAD_ALLOWED_ROOTS` env-var allowlist (default-deny); README "Security: filePath upload allowlist" section added |
| HI-03 — `send_image`/`send_animation` drop `**kwargs` | HIGH | HERMES-07 + HERMES-08 | CLOSED | `test_hi03_send_image_accepts_unknown_kwargs` + `test_hi03_send_animation_accepts_unknown_kwargs` xfail→PASS |
| HI-02 (if any in original review) | — | — | — | (no HI-02 in the carry-forward set per CODE-REVIEW.md scope — only HI-01 + HI-03) |

### MED findings closure

| Finding | Phase | Status |
|---------|-------|--------|
| MD-01 — three subtly-different success-shape coercions | HERMES-08 | CLOSED — `_coerce_success_payload` canonical helper |
| 02-MED-02 / IN-03 — conftest teardown gap | HERMES-11 | CLOSED — yield teardown + idempotency guard + `test_conftest_teardown.py` meta-tests |
| 06-MED-01 — smoke never starts a live gateway | HERMES-07 | CLOSED — live-loader test exercises `PluginContext` in-memory |
| GSD-MD-04 — test harness recorder pattern hid BL-01 | HERMES-07 | CLOSED — `test_base_process_message_invokes_keep_typing` exercises base pipeline without recorder |
| PR-MED-01 — `/health` route collision | HERMES-10 | CLOSED — `_validate_webhook_path` rejects `/health` |
| PR-MED-03 — smoke recompiles hermes-agent on every run | HERMES-11 | PARTIAL — `--retries 3` in; wheel caching deferred (documented) |
| PR-MED-04 — plugin.yaml phase-ID leak | HERMES-12 | CLOSED — `grep "HERMES-\d" plugin.yaml` returns no matches |
| 05-MED-01 — release docs | HERMES-12 | CLOSED — CHANGELOG 2.1.0 + README "What's new in v2.1" |

### LOW findings closure (selected — full per-phase tables in each VERIFICATION.md)

| Finding | Phase | Status |
|---------|-------|--------|
| 02-LOW-01 — silent error paths | HERMES-09 | CLOSED |
| 02-LOW-02 — send_typing log volume | HERMES-09 | CLOSED |
| 02-LOW-03 — get_chat_info `{}` semantics | HERMES-10 (docstring) + HERMES-12 (README) | CLOSED |
| 03-LOW-01 — webhook_path not validated | HERMES-10 | CLOSED |
| 04-LOW-02 — docs | HERMES-12 | CLOSED |
| 04-LOW-03 — initial-fire blocking | HERMES-08 | CLOSED |
| 05-LOW-01 — silent get_platform | HERMES-09 | CLOSED |
| 05-LOW-02 — chatId schema | HERMES-10 | CLOSED (permissive accept-set documented) |
| 05-LOW-03 — chatlytics_login semantics | HERMES-10 | CLOSED |
| 06-LOW-01 — smoke runtime | HERMES-11 | CLOSED (`--fast`) |
| 06-LOW-02 — first-fire WARNING | HERMES-08 | CLOSED |
| LO-11 — log levels | HERMES-09 | CLOSED |
| PR-LOW-03 — login UX | HERMES-10 | CLOSED |
| PR-LOW-06 — send_image vs send_image_file | HERMES-10 (doc-only) | CLOSED (collapse deferred to v2.2+ as breaking) |
| PR-INFO-02 — `_FakePlatformConfig` duplication | HERMES-11 | CLOSED |

## 3. Cross-phase integration

Cross-phase wiring is verified by the live-loader test harness introduced in HERMES-07 and exercised by every subsequent phase:

- `tests/test_live_loader.py::test_loader_registers_chatlytics_platform` — entry-point + register() wired correctly
- `tests/test_live_loader.py::test_loader_registers_21_tools` — full tool surface lands on the same PluginContext
- `tests/test_live_loader.py::test_base_process_message_invokes_keep_typing` — base `BasePlatformAdapter` inbound pipeline drives our `_keep_typing` override without crashing (BL-01 regression lock)
- `tests/test_concurrency.py` — `_resolve_media_url` off-event-loop + `_keep_typing` initial-fire non-blocking (cross-cutting between adapter and tool layer)
- `tests/test_observability.py` — log levels consistent across adapter/client/tools/inbound
- `tests/test_conftest_teardown.py` — platform registry hygiene across test sessions

No broken flows. Tool surface stable at exactly 21 tools across all six phases (asserted in `test_loader_registers_21_tools` AND `test_tool_count_matches_claude_code_plugin_baseline`).

## 4. Invariants preserved (8/8)

Per `ROADMAP.md` "v2.1 Architectural Invariants" block. Every phase's VERIFICATION.md re-asserted these explicitly:

1. Hermes pin `>=0.14,<0.15` — unchanged in pyproject.toml across all 6 phases
2. Inbound transport inside `connect()` via aiohttp (no Flask, no threads)
3. Tool surface = 21 tools (unchanged)
4. HTTP outbound through httpx async; aiohttp ONLY for embedded inbound
5. All tool handlers return `{"success": bool, ...}` shape (`_coerce_success_payload` enforces this canonically as of Phase 8)
6. Package name `chatlytics-hermes` preserved
7. MIT license preserved
8. NO PyPI publish (operator lock); v2.0 deliverables not regressed (45/45 v2.0 tests still pass within the 88/88 v2.1 suite)

## 5. Test totals

```
$ env -u CHATLYTICS_API_KEY -u CHATLYTICS_BASE_URL -u CHATLYTICS_ACCOUNT_ID \
      -u CHATLYTICS_WEBHOOK_HOST -u CHATLYTICS_WEBHOOK_PORT -u CHATLYTICS_WEBHOOK_PATH \
      -u CHATLYTICS_WEBHOOK_SECRET -u CHATLYTICS_HOME_CHANNEL -u CHATLYTICS_UPLOAD_ALLOWED_ROOTS \
      python -m pytest tests/ -q --no-header
88 passed in 22.52s
```

Test growth trajectory across the milestone:
- v2.0 baseline: 45
- Phase 7: 55 (10 new live-loader + xfail-strict regression tests)
- Phase 8: 58 (3 new concurrency tests, 6 xfail→PASS)
- Phase 9: 64 (6 new observability tests)
- Phase 10: 84 (19 new validation tests, +1 minor)
- Phase 11: 88 (4 new — `test_conftest_teardown.py` 2 + idempotency-related)
- Phase 12: 88 (no new tests; release-only)

Zero regressions across the milestone.

## 6. Release artifact state

- `pyproject.toml` version: `2.1.0`
- `plugin.yaml` version: `2.1.0`; zero `HERMES-\d` substring leaks
- `CHANGELOG.md` top entry: `## [2.1.0] -- 2026-05-17` (security-led, additive — no BREAKING marker)
- `README.md`: "What's new in v2.1" + "Known issues" + tool-catalog refresh
- `git tag --list`:
  - `v2.1.0` — present, local only, ready for operator push
  - `v2.0.0` — present, local only, points at known-broken artifact (BL-01 pre-fix); operator may delete after v2.1.0 push
  - `hermes-1.1.0` — legacy tag (pre-rebuild)

## 7. Operator lock compliance

Phase 12 VERIFICATION confirms:
- `tag_pushed_remote = false`
- `pypi_published = false`
- `github_release_created = false`
- No `python -m build`, `twine upload`, `gh release create`, or `git push` invoked anywhere in the milestone

## 8. Tech debt / deferred items (NOT blockers)

All items below are tracked and explicitly deferred — none block shipping v2.1.0:

- **Phase 9 LOW/INFO carry-forward** (LOW-01, INFO-02..04) — cosmetic log-level nits, non-blocking
- **Phase 10 LOW/INFO carry-forward** (LOW-02, INFO-01..03) — style nits + doc nits
- **`get_chat_info` sentinel `_error` key** — would be breaking, deferred to v2.2+
- **Strict JID regex enforcement** — would break phone numbers / display names, deferred to v2.2+
- **`send_image` / `send_image_file` collapse** — would be breaking, deferred to v2.2+
- **Smoke wheel caching** — long-term build optimization beyond `--retries 3`; deferred as future work
- **v2.0.0 local tag housekeeping** — operator may delete after v2.1.0 push (still-broken artifact superseded)

## 9. Audit verdict

**PASSED.** Ship v2.1.0 — proceed to `gsd-complete-milestone` and `gsd-cleanup`.

The carry-forward of deferred items into a future v2.2 milestone is a legitimate scoping decision and matches what the ROADMAP explicitly marks as "v2.2+ breaking changes." Nothing in the deferred set affects safety, correctness, or the operator-lock invariants of the v2.1.0 release artifact.
